name: Verify
on: workflow_dispatch

jobs:
  a:
    name: a
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Test ref
        run: echo ${{ github.sha }}
